server:
  port: 8081

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: attendance_db

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG